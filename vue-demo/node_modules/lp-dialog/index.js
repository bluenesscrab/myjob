//import Dialog from './Dialog'

// const install = (Vue) => {
//   Vue.component(Dialog.name, Dialog)
// }
const install = function (){
  console.log('LPDialog.install() this is a test dialog demo');
};

export default {
  install
}
